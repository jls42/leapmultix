<details>
<summary>本文档也有其他语言版本</summary>

- [English](./README.en.md)
- [Español](./README.es.md)
- [Português](./README.pt.md)
- [Deutsch](./README.de.md)
- [Français](./README.md)
- [हिन्दी](./README.hi.md)
- [العربية](./README.ar.md)
- [Italiano](./README.it.md)
- [Svenska](./README.sv.md)
- [Polski](./README.pl.md)
- [Nederlands](./README.nl.md)
- [Română](./README.ro.md)
- [日本語](./README.ja.md)
- [한국어](./README.ko.md)

</details>

# LeapMultix

<!-- Badges (update <owner>/<repo> after GitHub migration) -->

![CI](https://img.shields.io/github/actions/workflow/status/jls42/leapmultix/ci.yml?branch=main)

[![CodeFactor](https://www.codefactor.io/repository/github/jls42/leapmultix/badge)](https://www.codefactor.io/repository/github/jls42/leapmultix)
[![Codacy Badge](https://app.codacy.com/project/badge/Grade/fe7c2fbbea5e484889ac9b435c8d9956)](https://app.codacy.com/gh/jls42/leapmultix/dashboard?utm_source=gh&utm_medium=referral&utm_content=&utm_campaign=Badge_grade)
[![Quality Gate Status](https://sonarcloud.io/api/project_badges/measure?project=jls42_leapmultix&metric=alert_status)](https://sonarcloud.io/summary/new_code?id=jls42_leapmultix)

![License: AGPL v3](https://img.shields.io/badge/License-AGPL_v3-blue.svg)

## 目录

- [描述](#描述)
- [功能](#-功能)
- [快速开始](#-快速开始)
- [架构](#-架构)
- [游戏模式详情](#-游戏模式详情)
- [开发](#-开发)
- [兼容性](#-兼容性)
- [本地化](#-本地化)
- [数据存储](#-数据存储)
- [报告问题](#-报告问题)
- [许可证](#-许可证)

## 描述

LeapMultix 是一款现代化的交互式教育 Web 应用程序，旨在帮助儿童（8-12 岁）掌握 4 种算术运算：乘法 (×)、加法 (+)、减法 (−) 和除法 (÷)。该应用程序通过直观、无障碍且多语言的界面提供 **5 种游戏模式** 和 **4 种街机迷你游戏**。

**多运算支持：** 测验和挑战模式允许练习所有运算。探索、冒险和街机模式侧重于乘法，但也设计为支持所有运算。

**开发者：** Julien LS (contact@jls42.org)

**在线地址：** https://leapmultix.jls42.org/

## ✨ 功能

### 🎮 游戏模式

- **探索模式**：适应每种运算的视觉和交互式探索
- **测验模式**：支持所有 4 种运算 (×, +, −, ÷) 的多项选择题，具有自适应进度
- **挑战模式**：包含所有 4 种运算 (×, +, −, ÷) 和不同难度级别的计时赛
- **冒险模式**：通过关卡的叙事进度，支持所有 4 种运算

### 🕹️ 街机迷你游戏

- **MultiInvaders**：教育版太空侵略者 - 摧毁错误的答案
- **MultiMiam**：数学版吃豆人 - 收集正确的答案
- **MultiMemory**：记忆游戏 - 匹配运算和结果
- **MultiSnake**：教育版贪吃蛇 - 通过吃正确的数字成长

### ➕ 多运算支持

LeapMultix 在 **所有模式** 下提供 4 种算术运算的完整训练：

| 模式 | ×   | +   | −   | ÷   |
| ---- | --- | --- | --- | --- |
| 测验 | ✅  | ✅  | ✅  | ✅  |
| 挑战 | ✅  | ✅  | ✅  | ✅  |
| 探索 | ✅  | ✅  | ✅  | ✅  |
| 冒险 | ✅  | ✅  | ✅  | ✅  |
| 街机 | ✅  | ✅  | ✅  | ✅  |

### 🌍 跨领域功能

- **多用户**：具有保存进度的个人资料管理
- **多语言**：支持法语、英语和西班牙语
- **个性化**：头像、颜色主题、背景
- **无障碍**：键盘导航、触摸支持、符合 WCAG 2.1 AA 标准
- **移动端响应式**：针对平板电脑和智能手机优化的界面
- **进度系统**：分数、徽章、每日挑战

## 🚀 快速开始

### 先决条件

- Node.js（版本 16 或更高）
- 现代 Web 浏览器

### 安装

```bash
# 克隆项目
git clone https://github.com/jls42/leapmultix.git
cd leapmultix

# 安装依赖
npm install

# 启动开发服务器（选项 1）
npm run serve
# 应用程序将可通过 http://localhost:8080 访问（或下一个可用端口）

# 或使用 Python（选项 2）
python3 -m http.server 8000
# 应用程序将可通过 http://localhost:8000 访问
```

### 可用脚本

```bash
# 开发
npm run serve          # 本地服务器 (http://localhost:8080)
npm run lint           # 使用 ESLint 检查代码
npm run lint:fix       # 自动修复 ESLint 问题
npm run format:check   # 检查代码格式（提交前必须运行）
npm run format         # 使用 Prettier 格式化代码
npm run verify         # 质量门：lint + test + coverage

# 测试
npm run test           # 运行所有测试 (CJS)
npm run test:watch     # 监视模式下的测试
npm run test:coverage  # 带覆盖率报告的测试
npm run test:core      # 仅测试核心模块
npm run test:integration # 集成测试
npm run test:storage   # 存储系统测试
npm run test:esm       # ESM 测试 (tests-esm/ 文件夹, Jest vm-modules)
npm run test:verbose   # 详细输出的测试
npm run test:pwa-offline # PWA 离线测试（需要 Puppeteer），在 `npm run serve` 之后

# 分析和维护
npm run analyze:jsdoc  # 文档分析
npm run improve:jsdoc  # 自动改进 JSDoc
npm run audit:mobile   # 移动响应性测试
npm run audit:accessibility # 无障碍测试
npm run dead-code      # 未使用代码检测
npm run analyze:globals # 全局变量分析
npm run analyze:dependencies # 依赖使用分析
npm run verify:cleanup # 组合分析（死代码 + 全局变量）

# 资源管理
npm run assets:generate    # 生成响应式图像
npm run assets:backgrounds # 将背景转换为 WebP
npm run assets:analyze     # 响应式资源分析
npm run assets:diff        # 资源比较

# 国际化
npm run i18n:verify    # 验证翻译键一致性
npm run i18n:unused    # 列出未使用的翻译键
npm run i18n:compare   # 将翻译 (en/es) 与 fr.json (参考) 进行比较

# 构建和交付
npm run build          # 生产构建 (Rollup) + postbuild (完整的 dist/)
npm run serve:dist     # 在 http://localhost:5000 上服务 dist/（或可用端口）

# PWA 和 Service Worker
npm run sw:disable     # 禁用 service worker
npm run sw:fix         # 修复 service worker 问题
```

## 🏗️ 架构

### 文件结构

```
leapmultix/
├── index.html              # 主入口点
├── js/
│   ├── core/               # ES6 核心模块
│   │   ├── GameMode.js     # 模式基类
│   │   ├── GameModeManager.js # 游戏模式管理
│   │   ├── storage.js      # LocalStorage API
│   │   ├── audio.js        # 声音管理
│   │   ├── utils.js        # 通用实用程序（规范源）
│   │   ├── eventBus.js     # 事件通信
│   │   ├── userState.js    # 用户会话管理
│   │   ├── mainInit.js     # DOM 就绪初始化
│   │   ├── theme.js        # 主题系统
│   │   ├── userUi.js       # 用户界面实用程序
│   │   ├── parental.js     # 家长控制
│   │   ├── adventure-data.js # 冒险模式数据
│   │   ├── mult-stats.js   # 乘法统计
│   │   ├── challenge-stats.js # 挑战统计
│   │   └── daily-challenge.js # 每日挑战管理
│   ├── components/         # 可重用 UI 组件
│   │   ├── topBar.js       # 导航栏
│   │   ├── infoBar.js      # 游戏信息栏
│   │   ├── dashboard.js    # 用户仪表板
│   │   └── customization.js # 个性化界面
│   ├── modes/              # 游戏模式
│   │   ├── QuizMode.js
│   │   ├── ChallengeMode.js
│   │   ├── AdventureMode.js
│   │   ├── DiscoveryMode.js
│   │   └── ArcadeMode.js
│   ├── arcade/             # 街机迷你游戏
│   │   ├── arcade.js       # 主街机编排器
│   │   ├── arcade-invasion.js # 太空侵略者 (31 KB)
│   │   ├── arcade-multimemory.js # 记忆游戏 (31 KB)
│   │   ├── arcade-multimiam.js # MultiMiam 集成
│   │   ├── arcade-multisnake.js # 贪吃蛇集成
│   │   ├── arcade-common.js, arcade-utils.js # 共享实用程序
│   │   ├── arcade-message.js, arcade-points.js # UI 组件
│   │   └── arcade-scores.js # 分数管理
│   ├── multimiam/          # 吃豆人游戏（分解架构）
│   │   ├── multimiam.js    # 主控制器
│   │   ├── multimiam-engine.js # 游戏引擎 (15 KB)
│   │   ├── multimiam-renderer.js # 渲染系统 (9 KB)
│   │   ├── multimiam-controls.js # 控制管理 (7 KB)
│   │   ├── multimiam-questions.js # 问题生成 (6 KB)
│   │   └── multimiam-ui.js # 界面元素
│   ├── multisnake.js       # 贪吃蛇游戏 (38 KB)
│   ├── navigation/         # 导航系统
│   │   ├── slides.js       # 基于幻灯片的导航 (goToSlide, showSlide)
│   │   └── keyboard-navigation.js # 键盘支持
│   ├── ui/                 # 用户界面和反馈
│   │   ├── uiUtils.js      # 界面实用程序
│   │   ├── ui-feedback.js  # 反馈机制
│   │   ├── touch-support.js # 触摸支持 (7 KB)
│   │   ├── virtual-keyboard.js # 虚拟键盘
│   │   ├── coin-display.js, coin-effects.js # 货币系统
│   │   ├── notifications.js # 通知系统
│   │   └── badges.js       # 徽章系统
│   ├── media/              # 媒体管理
│   │   ├── VideoManager.js # 视频播放管理 (12 KB)
│   │   └── responsive-image-loader.js # 图像加载 (9 KB)
│   ├── orchestration/      # 编排和加载
│   │   ├── mode-orchestrator.js # 模式切换
│   │   ├── lazy-loader.js  # 动态加载 (10 KB)
│   │   └── game-cleanup.js # 状态清理
│   ├── utils/              # 实用程序
│   │   ├── utils-es6.js    # 主聚合器 (5 KB)
│   │   ├── main-helpers.js # 应用程序助手
│   │   ├── helpers.js      # 旧版辅助函数
│   │   ├── stats-utils.js  # 统计实用程序
│   │   ├── difficulty.js   # 难度管理
│   │   └── questionGenerator.js # 问题生成
│   ├── storage/            # 存储和状态
│   │   ├── storage.js      # 旧版存储包装器
│   │   └── userManager.js  # 多用户管理 (19 KB)
│   ├── i18n/               # 国际化
│   │   ├── i18n.js         # i18n 系统
│   │   └── i18n-store.js   # 翻译存储
│   ├── security/           # 安全和错误处理
│   │   ├── security-utils.js # XSS 保护，清理
│   │   ├── error-handlers.js # 全局错误处理
│   │   └── logger.js       # 日志系统
│   ├── accessibility/      # 无障碍
│   │   ├── accessibility.js # 无障碍功能
│   │   └── speech.js       # 语音合成支持
│   ├── integration/        # 集成和分析
│   │   ├── plausible-init.js # Plausible 分析
│   │   ├── cache-updater.js # 缓存管理 (10 KB)
│   │   └── imports.js      # 导入实用程序
│   ├── main-es6.js         # ES6 入口点
│   ├── main.js             # 主编排器
│   ├── bootstrap.js        # ES6 事件处理程序设置
│   └── game.js             # 状态管理和每日挑战
├── css/                    # 模块化样式
├── assets/                 # 资源
│   ├── images/             # 图像和精灵
│   ├── generated-images/   # 生成的响应式图像
│   ├── sounds/             # 音效
│   ├── translations/       # 翻译文件 (fr, en, es)
│   └── videos/             # 教程视频
├── tests/                  # 自动化测试
│   ├── __tests__/          # 单元和集成测试
│   └── tests-esm/          # ESM 测试 (.mjs)
├── scripts/                # 维护脚本
│   ├── compare-translations.cjs # 翻译比较
│   └── cleanup-i18n-keys.cjs # i18n 键清理
└── dist/                   # 生产构建（生成）
```

### 技术架构

**现代 ES6 模块**：该项目使用具有 ES6 类和原生导入/导出的模块化架构。

**可重用组件**：使用集中式 UI 组件（TopBar, InfoBar, Dashboard, Customization）构建的界面。

**延迟加载**：通过 `lazy-loader.js` 按需智能加载模块，以优化初始性能。

**统一存储系统**：通过具有回退功能的 LocalStorage 进行用户数据持久化的集中式 API。

**集中式音频管理**：具有多语言支持和每位用户首选项的声音控制。

**事件总线**：组件之间解耦的事件驱动通信，用于可维护的架构。

**幻灯片导航**：基于编号幻灯片（slide0, slide1 等）的导航系统，使用 `goToSlide()`。

**安全**：通过 `security-utils.js` 进行 XSS 保护和清理，用于所有 DOM 操作。

## 🎯 游戏模式详情

### 探索模式

乘法表的可视化探索界面，具有：

- 乘法的交互式可视化
- 动画和记忆辅助
- 教育性拖放
- 每张表的自由进度

### 测验模式

多项选择题，具有：

- 每次会话 10 个问题
- 基于成功的自适应进度
- 虚拟数字键盘
- 连胜系统（连续正确答案）

### 挑战模式

计时赛，具有：

- 3 个难度级别（初级、中级、高级）
- 正确答案的时间奖励
- 生命系统
- 最佳分数排行榜

### 冒险模式

叙事进度，具有：

- 12 个可解锁的主题关卡
- 具有视觉进度的交互式地图
- 具有角色的沉浸式故事
- 星星系统和奖励

### 街机迷你游戏

每个迷你游戏提供：

- 难度选择和个性化
- 生命系统和分数
- 键盘和触摸控制
- 每位用户的个人排行榜

## 🛠️ 开发

### 开发工作流

**重要：切勿直接提交到 main**

该项目使用基于功能分支的工作流：

1.  **创建分支**：

    ```bash
    git checkout -b feat/feature-name
    # 或
    git checkout -b fix/bug-name
    ```

2.  **开发和测试**：

    ```bash
    npm run format:check  # 务必先检查格式
    npm run format        # 如有必要进行格式化
    npm run lint          # 检查代码质量
    npm run test          # 运行测试
    npm run test:coverage # 检查覆盖率
    ```

3.  **提交到分支**：

    ```bash
    git add .
    git commit -m "feat: description of the feature"
    ```

4.  **推送并创建 Pull Request**：
    ```bash
    git push -u origin feat/feature-name
    ```

**提交风格**：简洁，祈使语气（例如："Fix arcade init errors", "Refactor cache updater"）

**质量门**：确保 `npm run lint`, `npm test` 和 `npm run test:coverage` 在每次提交前通过

### 组件架构

**GameMode（基类）**：所有模式都继承自具有标准化方法的公共类。

**GameModeManager**：模式启动和管理的集中编排。

**UI 组件**：TopBar, InfoBar, Dashboard 和 Customization 提供一致的界面。

**延迟加载**：模块按需加载以优化初始性能。

**事件总线**：组件之间通过事件系统进行解耦通信。

### 测试

该项目包含一套全面的测试套件：

- 核心模块的单元测试
- 组件的集成测试
- 游戏模式测试
- 自动化代码覆盖率

```bash
npm test              # 所有测试 (CJS)
npm test:core         # 核心模块测试
npm test:integration  # 集成测试
npm test:coverage     # 覆盖率报告
npm run test:esm      # ESM 测试（例如 components/dashboard）通过 vm-modules
```

### 生产构建

- **Rollup**：将 `js/main-es6.js` 捆绑到具有代码拆分和 sourcemap 的 ESM 中
- **Terser**：自动缩小以进行优化
- **Post-build**：复制 `css/` 和 `assets/`，favicons (`favicon.ico`, `favicon.png`, `favicon.svg`)，`sw.js`，并将 `dist/index.html` 重写为哈希入口文件（例如：`main-es6-*.js`）
- **最终文件夹**：`dist/` 准备好进行静态服务

```bash
npm run build      # 生成 dist/
npm run serve:dist # 服务 dist/ (端口 5000)
```

### 持续集成

**GitHub Actions**：`.github/workflows/ci.yml` 中的自动化管道

CI/CD 管道在每次推送和拉取请求时自动运行：

**主要作业**：

1.  **build-test**：主要验证作业
    - 安装依赖：`npm ci`
    - 检查格式：`npm run format:check`
    - 静态分析：`npm run lint`
    - 单元测试：`npm run test`
    - 安全审计：`npm audit`
    - 覆盖率工件生成

2.  **accessibility**：无障碍审计（非阻塞）
    - 运行 `npm run audit:accessibility`
    - 生成 WCAG 2.1 AA 无障碍报告

3.  **test-esm**：ES6 模块测试
    - 运行 `npm run test:esm` 使用 Jest VM modules
    - 验证 ES6 导入/导出

4.  **lighthouse**：性能审计（非阻塞）
    - 移动性能审计
    - 生成 Lighthouse 报告工件
    - Core Web Vitals 指标

**质量徽章**：

- CI Build Status (GitHub Actions)
- CodeFactor Grade
- Codacy Badge
- SonarCloud Quality Gate

### PWA (Progressive Web App)

LeapMultix 是一个完整的 PWA，具有离线支持和安装能力。

**Service Worker** (`sw.js`)：

- 导航：网络优先，离线回退到 `offline.html`
- 图像：缓存优先以优化性能
- 翻译：Stale-while-revalidate 用于后台更新
- JS/CSS：网络优先以始终服务最新版本
- 通过 `cache-updater.js` 进行自动版本管理

**Manifest** (`manifest.json`)：

- 适用于所有设备的 SVG 和 PNG 图标
- 可在移动设备上安装（添加到主屏幕）
- 用于类似应用程序体验的独立配置
- 支持主题和颜色

**本地测试离线模式**：

1.  启动开发服务器：

    ```bash
    npm run serve
    ```

    打开 `http://localhost:8080`（或显示的端口）

2.  手动测试：
    - 在 DevTools 中切断网络（Network 选项卡 → Offline）
    - 刷新页面 → 显示 `offline.html`

3.  自动化测试（需要 Puppeteer）：
    ```bash
    npm run test:pwa-offline
    ```

**Service Worker 管理脚本**：

```bash
npm run sw:disable  # 禁用 service worker
npm run sw:fix      # 修复缓存问题
```

### 质量标准

**代码质量工具**：

- **ESLint**：具有扁平配置 (`eslint.config.js`) 的现代配置，ES2022 支持
- **Prettier**：自动代码格式化 (`.prettierrc`)
- **Stylelint**：CSS 验证 (`.stylelintrc.json`)
- **JSDoc**：具有覆盖率分析的自动函数文档

**重要代码规则**：

- 删除未使用的变量和参数 (`no-unused-vars`)
- 使用特定的错误处理（无空 catch）
- 避免 `innerHTML`，支持 `security-utils.js` 函数
- 保持函数的认知复杂度 < 15
- 将复杂函数提取为较小的助手

**安全**：

- **XSS 保护**：使用来自 `security-utils.js` 的函数：
  - `appendSanitizedHTML()` 代替 `innerHTML`
  - `createSafeElement()` 创建安全元素
  - `setSafeMessage()` 用于文本内容
- **外部脚本**：`crossorigin="anonymous"` 属性强制
- **输入验证**：始终清理外部数据
- **Content Security Policy**：CSP 标头限制脚本源

**无障碍**：

- WCAG 2.1 AA 合规
- 全键盘导航
- ARIA 角色和适当的标签
- 符合标准的颜色对比度

**性能**：

- 通过 `lazy-loader.js` 延迟加载模块
- CSS 和响应式资源优化
- 用于智能缓存的 Service Worker
- 生产中的代码拆分和缩小

## 📱 兼容性

### 支持的浏览器

- Chrome/Chromium 90+
- Firefox 88+
- Safari 14+
- Edge 90+

### 设备

- **桌面**：键盘和鼠标控制
- **平板电脑**：优化的触摸界面
- **智能手机**：自适应响应式设计

### 无障碍

- 全键盘导航（Tab, 箭头, Esc）
- 用于屏幕阅读器的 ARIA 角色和标签
- 符合标准的颜色对比度
- 辅助技术支持

## 🌍 本地化

完全多语言支持：

- **法语**（默认语言）
- **英语**
- **西班牙语**

### 翻译管理

**翻译文件：** `assets/translations/*.json`

**格式：**

```json
{
  "menu_start": "开始",
  "quiz_correct": "做得好！",
  "arcade_invasion_title": "MultiInvaders"
}
```

### i18n 管理脚本

**`npm run i18n:verify`** - 验证翻译键一致性

**`npm run i18n:unused`** - 列出未使用的翻译键

**`npm run i18n:compare`** - 将翻译文件与 fr.json（参考）进行比较

此脚本 (`scripts/compare-translations.cjs`) 确保所有语言文件的同步：

**功能：**

- 检测缺失的键（在 fr.json 中存在但在其他语言中缺失）
- 检测额外的键（在其他语言中存在但不在 fr.json 中）
- 识别空值 (`""`, `null`, `undefined`, `[]`)
- 类型一致性检查（字符串 vs 数组）
- 将嵌套 JSON 结构扁平化为点表示法（例如 `arcade.multiMemory.title`）
- 生成详细的控制台报告
- 将 JSON 报告保存到 `docs/translations-comparison-report.json`

**输出示例：**

```
🔍 翻译文件比较分析

📚 参考语言：fr.json
✅ fr.json: 335 个键

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 en.json 分析
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 总键数：335
✅ 无缺失键
✅ 无额外键
✅ 无空值

📊 最终摘要
  fr.json: 335 个键
  en.json: 335 个键
  es.json: 335 个键

✅ 所有翻译文件都完美同步！
```

**翻译覆盖范围：**

- 完整的用户界面
- 游戏说明
- 错误和反馈消息
- 描述和上下文帮助
- 冒险模式叙事内容
- 无障碍和 ARIA 标签

## 📊 数据存储

### 用户数据

- 个人资料和偏好
- 每个游戏模式的进度
- 街机游戏分数和统计
- 个性化设置

### 技术功能

- 具有回退功能的本地存储 (localStorage)
- 每位用户的数据隔离
- 自动进度保存
- 旧数据的自动迁移

## 🐛 报告问题

问题可以通过 GitHub issues 报告。请包括：

- 问题的详细描述
- 复现步骤
- 浏览器和版本
- 屏幕截图（如果相关）

## 💝 支持项目

**[☕ 通过 PayPal 捐赠](https://paypal.me/jls)**

## 📄 许可证

此项目根据 AGPL v3 许可证授权。有关更多详细信息，请参阅 `LICENSE` 文件。

---

_LeapMultix - 学习乘法表的现代教育应用程序_
